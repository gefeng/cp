#include <algorithm>
#include <array>
#include <cmath>
#include <iostream>
#include <vector>

using namespace std;

void run_case() {
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    
    int T = 0;
    cin >> T;
    while(T--) {
        run_case();
    }
}
